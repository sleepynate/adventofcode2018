package adventofcode

import org.scalatest.FunSuite

class ExampleSuite extends FunSuite {
  test("we can write a test") {
    assert(new ExampleClass().getValue() === 0, "Make sure the tests can fail")
  }
}
